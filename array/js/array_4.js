//  Метод (symbIndex) для вывода символов из обычной строки.

let str = "Array!";

for (let symbIndex in str) {
  console.log(str[symbIndex]);
  // alert (str[symbIndex]);
}

//  Вариант № 2

let str_1 = "World!";

for (let symb of str_1) {
  console.log (symb);
}

 

// Тоже для вывода индехса строки.

let str_2 = "Hello";

for (let symbIndex in str_2) {
  console.log (symbIndex);
}


